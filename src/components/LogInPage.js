import React from "react";

const styles = {
    backgroundColor: '#000000',
    margin: 0,
    padding: 0,
    height: '100vh',
  };

const LogInPage = () => {
    
      return (
        <div style={styles}></div>
      );
};

export default LogInPage;
